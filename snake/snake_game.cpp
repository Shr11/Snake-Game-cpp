#include <bits/stdc++.h>  // for generating random numbers (ex. food for snake ) and for arrays and stuff
#include <conio.h>  // generating triggers on keyboard hits with keyboard hit library (kbhit func)
#include <windows.h>  // drawing characters at different coordinates on console, not only (0,0)(default)

using namespace std;

#define MAX_LENGTH 1000

// Directions (read-only global vars)

const char DIR_UP = 'U';
const char DIR_DOWN = 'D';
const char DIR_LEFT = 'L';
const char DIR_RIGHT = 'R';

// global console window size vars

int consoleWidth , consoleHeight;

void initScreen()
{
    HANDLE hConsole = GetStdHandle(STD_OUTPUT_HANDLE);
    
}


int main()
{



return 0;
}
